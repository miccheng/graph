package com.example.leetcode.tree;

public class Successor {
    //find the next node(in order traversal) of a given node in a binary tree. Assume node has link to its parent node

    /*    if(n has a right subtree){
        find the left most node of its right subtree
    }else{//go up until n is the left of the parent
        while(n is right child of parent){
            n=n.parent;
        }
        return n.parent;
    }*/
}
