1. !!!Tree + pointer
    BST iterator
    convert BST to a double liked list

2. DFS:
    base case: MinCostPathTree

3. BST:
   can used both method signature or global variable for tracking
   > keep track of min and max
   > keep track of min and move pointers
   > use stack
   > range query : exclude/include left/right branch. eg. range sum of BST, Closest Binary Search Tree Value

4. leaf node
   > base condition
   if(root==null)
   if(root.left==null&&root.right==null)

5. Pointer in tree
   ConvertBST2SortedDoublyLinkedList
   BSTIterator
   Flatten Binary Tree to Linked List
   PopulatingNextRightPointersInTree

6. String 2 Tree problem
   VerifyPreorderInBST
   ConstructBSTFromPreorder
   SerializeDeserializeBST
   PopulatingNextRightPointersInTree

7.post order
  Delete Nodes And Return Forest
  Smallest Subtree with all the Deepest Nodes

8. from string to BST:
    construct the root first
    serialize-and-deserialize-bst
    constructBSTFromString